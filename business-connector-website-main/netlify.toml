[build]
  # Ensure pnpm is available in Netlify build image
  command = "npm install -g pnpm && pnpm install --frozen-lockfile=false && pnpm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
